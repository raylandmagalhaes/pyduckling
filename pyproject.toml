[build-system]
requires = ["maturin"]
build-backend = "maturin"

[tool.maturin]
manylinux = "off"
sdist-include = ["duckling-ffi/*"]

[project]
name = "pyduckling_native"  # Replace with your actual package name
version = "0.1.0"   
